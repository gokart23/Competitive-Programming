#include "bits/stdc++.h"

using namespace std;

typedef unsigned long long int ulint;
typedef long long int lint;

int main(int argc, char const *argv[])
{
	ulint testcases=0, n=0, k=0;

	scanf("%llu", &testcases);

	while(testcases--)
	{
		ulint counter=0, temp=0;
		bool flag = true;
		scanf("%llu%llu", &n, &k);
		while(n--)
		{
			if(flag)
			{
				scanf("%llu", &temp);
				if(temp)
			}
		}
	}
	return 0;
}